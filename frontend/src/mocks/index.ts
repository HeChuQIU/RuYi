/**
 * Mock 配置入口
 */

// TODO: 导入 Mock handlers
// import { userMocks } from './handlers/user'
// import { modelMocks } from './handlers/model'

/**
 * 注册所有 Mock handlers
 */
export function registerMockHandlers() {
  // TODO: 注册各个模块的 Mock handlers
  // userMocks.forEach(mock => mockLink.addMock(mock.operationName, mock))
  // modelMocks.forEach(mock => mockLink.addMock(mock.operationName, mock))
}

