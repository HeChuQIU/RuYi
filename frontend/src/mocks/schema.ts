/**
 * GraphQL Schema 定义（用于 Mock）
 */

// TODO: 定义 GraphQL Schema
// 可以使用 graphql-tag 或直接使用字符串定义
// 示例：
// export const typeDefs = gql`
//   type User {
//     id: ID!
//     username: String!
//     email: String!
//   }
//   type Query {
//     users: [User!]!
//   }
// `

